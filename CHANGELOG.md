## 0.0.1

- Initial release of `facerd_flutter`.
- Provides `FaceRDPlugin` for triggering FaceRD face capture with `PidOptions`.
- Adds `RDDetector` helper for checking FaceRD RD device availability.
